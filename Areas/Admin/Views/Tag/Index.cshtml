@{
	int count = 1;
}

@model List<Tag>

<div class="container-fluid pt-4 px-4">
	<div class="col-sm-12 col-xl-12">
		<div class="bg-secondary rounded p-4">
			@if (Model.Count == 0)
			{
				<h3>No Tag found</h3>
				<a asp-action="Create" asp-controller="Tag">Create Tag</a>
			}
			else
			{
				<h3>Tags</h3>
				<a asp-action="Create" asp-controller="Tag">Create</a>
				<table class="table">
					<tr>
						<th scope="col">#</th>
						<th scope="col">Name</th>
						<th scope="col">Actions</th>
					</tr>
					@foreach (var item in Model)
					{
						<tr>
							<th scope="row">@count</th>
							<td>@item.Name</td>
							<td>
								<a asp-action="Details" asp-controller="Tag" asp-route-id="@item.Id">Details</a> |
								<a asp-action="Edit" asp-controller="Tag" asp-route-id="@item.Id">Edit</a> |
								<a asp-action="Delete" asp-controller="Tag" asp-route-id="@item.Id">Delete</a>
							</td>
						</tr>
						count++;
					}
				</table>
			}
		</div>
	</div>
</div>